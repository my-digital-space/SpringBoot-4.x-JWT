{
  "name": "JWT Auth — Local",
  "_postman_id": "jwt-auth-local-environment-v1",
  "values": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8081",
      "type": "default",
      "enabled": true,
      "description": "Base URL of the running Spring Boot application. Change port if SERVER_PORT env var differs."
    },
    {
      "key": "accessToken",
      "value": "",
      "type": "secret",
      "enabled": true,
      "description": "JWT access token. Auto-populated by the Login and Register test scripts. Valid for 10 minutes."
    },
    {
      "key": "adminAccessToken",
      "value": "",
      "type": "secret",
      "enabled": true,
      "description": "JWT access token for the ADMIN user. Auto-populated by the Admin Login test script."
    },
    {
      "key": "orderId",
      "value": "",
      "type": "default",
      "enabled": true,
      "description": "ID of the last created order. Auto-populated by the Create Order test script. Used by Get, Update, and Delete requests."
    }
  ],
  "schema": "https://schema.getpostman.com/json/environment/v2.1.0/environment.json"
}
